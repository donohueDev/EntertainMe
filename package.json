{
  "name": "entertainme",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "install": "npm install --prefix backend",
    "start": "npm run start --prefix backend",
    "dev": "npm run dev --prefix backend"
  }
}